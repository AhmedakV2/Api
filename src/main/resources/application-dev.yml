spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG

aft:
  jwt:
    secret: ${AFT_JWT_SECRET:gelistirme-ortami-icin-en-az-otuz-iki-karakterlik-anahtar}
