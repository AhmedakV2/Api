spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG

aft:
  jwt:
    secret: ${AFT_JWT_SECRET:}

spring:
  ai:
    ollama:
      base-url: ${AFT_OLLAMA_URL:http://localhost:11434}
