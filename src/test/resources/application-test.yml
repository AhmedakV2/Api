spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  data:
    redis:
      host: localhost

aft:
  cors:
    allowed-origins: http://localhost
  jwt:
    secret: test-anahtari-en-az-otuz-iki-karakter-uzunlugunda
